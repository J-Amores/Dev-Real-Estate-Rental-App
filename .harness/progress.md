# Progress log

- 2026-06-09 [Architect] Skeleton scaffolded + verified GREEN. Committed pre-port Next.js snapshot (c105e19 — M0 baseline source; capture .harness/baselines/ from `npx pnpm next:dev` BEFORE deleting legacy sources). Astro 5 + @astrojs/preact 4 + @tailwindcss/vite skeleton in `src/` (pinned to Astro 5: Node here is 20.x, Astro 6 needs ≥22.12): Layout.astro (EVASION meta/favicons/Inter via @fontsource), placeholder index.astro with required anchor ids, verbatim globals.css port, `src/lib/{types,utils}.ts`, PLACEHOLDER data layer `src/lib/data/properties.ts` (5 seeds p-001…p-005), stub `/properties` + `/properties/[id]`. Legacy Next app kept runnable via `next:dev|build|start` scripts on frozen `tsconfig.next.json`; legacy dirs excluded from Astro tsconfig. Verified: `npx pnpm build` ✓ (7 routes), `npx pnpm check` ✓ (0/0/0), preview health ✓ (/, /properties, /properties/p-001 → 200). Substrate instantiated: CLAUDE.md + CODEBASE_MAP.md rewritten with real values; asset-models.json already concrete in .claude/harness-substrate/. NOTE: live `.claude/settings.json` edit (adding `Edit(./dist/**)`+`Edit(./build/**)` deny rules from the settings template) was denied by the permission classifier — existing settings already deny lock/env edits and ask on push/publish/rm -rf; remaining delta needs a human or builder with grant. Wrote .harness/api-contract.md. Next: M0 baseline capture, then M2/M3 section port.
- 2026-06-09 [Frontend] M2+M3 sections ported and verified GREEN. Islands under `src/components/islands/` (Preact): Header (client:load — scroll pill + exact layered shadow + lucide-preact Menu/X mobile menu), HeroSection (client:load — sticky 200vh MONO letter stagger + center/side interpolation + fixed tagline fade, id="hero" added per contract), PhilosophySection (client:visible — 3-title rotateX flip + word blur reveal), TechnologySection (client:visible — 400vh day-cycle crossfade + 3 text cycles + ScrollRevealText), GallerySection (client:visible — rAF card stacking + last-image fullscreen expansion), FadeImage (IO + load-state fade, props {src, alt, class}, cache-complete guard). Static `.astro` under `src/components/sections/`: FeaturedProductsSection (bento grid, 10 FadeImage islands), CollectionSection (mobile snap carousel + desktop grid), EditorialSection (parallax via vanilla rAF <script> — no hydration), TestimonialsSection, FooterSection (id="reserve" added per contract). index.astro composes old app/page.tsx order; all next/image fill → absolute object-cover <img>, Link → <a>. Verified: `npx pnpm build` ✓ (7 routes), `npx pnpm check` ✓ (0/0/0), Playwright on preview ✓ — 0 console errors (desktop 1440×900 + mobile 390×844), all anchor ids present (hero/products/technology/gallery/accessories/about/reserve), 21 astro-islands hydrate, header pill activates >50px, hero centerWidth interpolates (45% mid-scroll), gallery stacking verified at progress 0.625, mobile menu toggles (5 links), /properties lists 5 seed cards. NOTE: legacy components/ + app/ sources intentionally NOT deleted — M0 baselines (.harness/baselines/) still uncaptured and require `npx pnpm next:dev`; delete legacy + finish M3 cleanup only after baseline capture.
- 2026-06-09 [Deployer] Deploy-prepare COMPLETE (Vercel, prepare-only — nothing deployed/pushed). Artifacts: `vercel.json` (framework astro, frozen-lockfile install, dist/ output, cleanUrls, immutable /_astro/* cache headers); `.github/workflows/ci.yml` (pnpm 10 + Node 20 → astro check → astro build → route assertions for /, /properties, /properties/[id] → dist artifact upload); `env.example` (no required vars — static site; written undotted because permission rules deny `.env*` writes by ANY tool, incl. Bash heredoc — human must `mv env.example .env.example`); `Dockerfile` + `.dockerignore` (portable fallback: node:20-alpine corepack-pnpm build → nginx:1.27-alpine serving dist/). No @astrojs/vercel adapter added — static output needs none. Verified dist/ layout matches CI route assertions (dist/properties/p-001..p-005/index.html present). Exact human deploy steps in `.harness/deploy-plan.md` (commit artifacts → `npx vercel deploy` → verify preview → `npx vercel deploy --prod`).
- M1 cleanup (post-eval): legacy Next.js sources + deps purged (app/, components/, hooks/, lib/, styles/, next.config.mjs, postcss.config.mjs, tsconfig.next.json, components.json, next-env.d.ts; deps next/react/react-dom/lucide-react/Radix et al.). Re-verified: astro build 7 pages, astro check 0/0/0, preview 200 on / /properties /properties/p-001. CLAUDE.md + CODEBASE_MAP.md refreshed. stack-purge gate failure resolved.
- 2026-06-09 [Human-granted deploy] First Vercel deploy executed (user granted "deploy the preview"). CLI v53 tagged the fresh project's first deployment target=production; user accepted as production v1. Live: https://real-estate-app-dun-ten.vercel.app (project jadev-projects/real-estate-app, .vercel/ created). Git remotes intentionally NOT connected (no push-to-deploy). Smoke: / /properties /properties/p-001 → 200 on the live URL.
